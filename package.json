{
  "name": "pex-fullstack",
  "version": "1.0.0",
  "description": "The Personal Exchange - Full Stack Application",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm start",
    "install-deps": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
